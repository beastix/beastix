PWD             != pwd
SRC_ROOT        := ${PWD}/..
PORTS_ROOT      := ~/beastix-ports
BEASTIX_VERSION := 0.1
BEASTIX_BRANCH  := CURRENT
RELEASE_BUILD   := ${SRC_ROOT}/releng/build
RELEASE_OUTPUT  := ${SRC_ROOT}/releng/release
RELEASE_FINAL   := ${SRC_ROOT}/releng/release/Beastix-${BEASTIX_VERSION}-${BEASTIX_BRANCH}
